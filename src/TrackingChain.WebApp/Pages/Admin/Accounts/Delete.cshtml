@page
@model TrackingChain.TriageWebApplication.Pages.Admin.Accounts.DeleteModel

@if (!Model.AlreadyUsedAccount)
{
    <h3>Are you sure you want to delete this?</h3>
    <div>
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.AccountBinding.Id)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.AccountBinding.Id)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.AccountBinding.Name)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.AccountBinding.Name)
            </dd>
        </dl>

        <form method="post">
            <input type="hidden" asp-for="AccountBinding.Id" />
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a asp-page="./Index">Back to List</a>
        </form>
    </div>
}
else
{
    <br />
    <br />
    <h3>You cannot remove Account that have already linked to Profile Group</h3>
    <br />
    <a asp-page="./Index">Back to List</a>
    <br />
}